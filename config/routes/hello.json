{
    "name": "hello",
    "handler": {
        "type": "ScriptableHandler",
        "config": {
            "type": "application/x-groovy",
            "file": "hello.groovy"
        }
    },
    "condition": "${matches(request.uri.path, '^/hello$')}"
}